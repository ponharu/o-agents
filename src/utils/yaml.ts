/**
 * YAML stringify options:
 * - `blockQuote: 'literal'`: Use pipe (`|`) for multi-line strings
 * - `lineWidth: 0`: Disable line width limit
 */
export const yamlStringifyOptions = { blockQuote: "literal", lineWidth: 0 } as const;
